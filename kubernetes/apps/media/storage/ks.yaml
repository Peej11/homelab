---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: &app media-storage
  namespace: media
  labels:
    app.kubernetes.io/name: *app
    app.kubernetes.io/part-of: &namespace media
spec:
  dependsOn:
    - name: flux-system
      namespace: flux-system
    - name: rook-ceph-config
      namespace: flux-system
  decryption:
    provider: sops
    secretRef:
      name: sops-age
  commonMetadata:
    labels:
      app.kubernetes.io/name: *app
      app.kubernetes.io/namespace: *namespace
  interval: 4h
  path: ./kubernetes/apps/media/storage
  prune: true
  retryInterval: 5m
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  timeout: 10m
  wait: true
