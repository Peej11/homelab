---
apiVersion: notification.toolkit.fluxcd.io/v1beta3
kind: Provider
metadata:
  name: discord
  namespace: monitoring
spec:
  type: discord
  secretRef:
    name: discord-webhook
---
apiVersion: notification.toolkit.fluxcd.io/v1beta3
kind: Alert
metadata:
  name: kube-prometheus-stack-kustomization-alert
  namespace: monitoring
spec:
  summary: "Kube-Prometheus-Stack"
  providerRef:
    name: discord
  eventSeverity: info
  eventSources:
    - kind: Kustomization
      name: 'kube-prometheus-stack'
      namespace: monitoring
