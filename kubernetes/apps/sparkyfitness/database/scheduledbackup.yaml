---
apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: sparkyfitness-scheduled-backup
  namespace: sparkyfitness
spec:
  immediate: true
  schedule: "0 0 7 * * *"
  backupOwnerReference: self
  cluster:
    name: sparky
  method: volumeSnapshot
  target: primary
