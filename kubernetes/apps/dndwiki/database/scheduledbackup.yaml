---
apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: dndwiki-scheduled-backup
  namespace: dndwiki
spec:
  immediate: true
  schedule: "0 0 7 * * *"
  backupOwnerReference: self
  cluster:
    name: dndwiki
  method: volumeSnapshot
  target: primary
