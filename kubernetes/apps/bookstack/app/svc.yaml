---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: bookstack
  name: dndwiki-bookstack
  namespace: dndwiki
spec:
  ports:
    - name: web-ui
      port: 8080
      targetPort: 8080
  selector:
    app.kubernetes.io/name: bookstack
